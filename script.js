

const titleScreen = (() => {

    // Saving HTML DOM elements to be used 
    const startbutton = document.querySelector('.startGame');
    const cardplayer = document.querySelector('.player');
    const cardbot = document.querySelector('.bot');
    const vs = document.querySelector('.vs');
    const titleContainer = document.querySelector('.main');
    const header = document.querySelector('header');

    //Button animatios for looding screen
    
})